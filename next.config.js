/** @type {import('next').NextConfig} */
// const images = {
//   domains: ["cdn.traction.one"]
// }

const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.traction.one']
  }
}

module.exports = nextConfig

